function git2zhenjun() {
  git config user.name "Zhenjun10"
  git config user.email "yuanyixincun@163.com"
}

function lrun() {
  ./scripts/run.sh
}

function llaunch() {
  ./scripts/launch.sh
}

function lcompile() {
  ./scripts/compile.sh
}

function zle-keymap-select {
  if [[ $KEYMAP == vicmd ]]; then
    echo -ne '\e[1 q'   # 方块光标（Normal）
  else
    echo -ne '\e[5 q'   # 竖线光标（Insert）
  fi
}
zle -N zle-keymap-select
